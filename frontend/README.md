# 🗺️ Карта Здоровья — Тула

Фронтенд приложения для TulaHackDays 2025. Интерактивная карта здоровых мест города.

## 🚀 Быстрый старт

```bash
# Установка зависимостей
npm install

# Запуск в dev-режиме (с прокси на бэкенд)
npm run dev

# Сборка для продакшена
npm run build
```

## 🛠️ Технологии

- ⚛️ **React 18** + **TypeScript**
- 🎨 **Tailwind CSS** — стилизация
- 🗺️ **React-Leaflet** + **Leaflet.markercluster** — карта и маркеры
- 📦 **Zustand** — state management
- ⚡ **Vite** — сборка

## 📁 Структура проекта

```
src/
├── api/              # API клиент (axios)
│   └── index.ts
├── components/       # UI компоненты
│   ├── Header.tsx
│   ├── MapView.tsx
│   ├── ListView.tsx
│   ├── BottomSheet.tsx
│   ├── ReviewForm.tsx
│   ├── AuthModal.tsx
│   └── FilterModal.tsx
├── pages/            # Страницы
│   └── HomePage.tsx
├── store/            # Zustand store
│   └── index.ts
├── types/            # TypeScript типы
│   └── index.ts
├── App.tsx
├── main.tsx
└── index.css
```

## ✨ Функциональность

### Реализовано
- ✅ Карта с маркерами объектов (кластеризация)
- ✅ Переключение карта/список
- ✅ Просмотр информации об объекте
- ✅ Два типа рейтинга: Health Score (0-100) и User Rating (1-5 звёзд)
- ✅ Регистрация и авторизация
- ✅ Добавление отзывов с фото
- ✅ Лайки/дизлайки отзывов
- ✅ Фильтрация по типу и характеристикам
- ✅ Загрузка фото (MinIO)

### В разработке
- 🚧 Добавление новых объектов
- 🚧 Профиль пользователя
- 🚧 Лидерборд

## 🔌 API

Backend: `http://85.198.80.80:8000/api`

В dev-режиме запросы проксируются через Vite.

### Основные эндпоинты
- `GET /place/` — все объекты
- `GET /place/search` — поиск с фильтрами
- `GET /place/types` — типы объектов
- `POST /user/create` — регистрация
- `POST /user/login` — авторизация
- `POST /user/review` — добавить отзыв (с rating 1-5 и photos[])
- `POST /review/rank` — лайк/дизлайк отзыва
- `POST /photo/upload` — загрузка фото

## 🎨 Дизайн

- Mobile-first подход
- Bottom sheet для информации об объекте
- Маркеры с эмодзи по типу места
- Цветовая индикация: зелёный (здоровое), жёлтый (нейтральное), красный (нездоровое)

## 👥 Команда

- 👨‍💻 Денис — Frontend
- 👨‍💻 Никита — Backend
- 👩‍💻 Ангелина — DevOps
- 🎨 Даня — Design
- 🤖 Леша — ML
